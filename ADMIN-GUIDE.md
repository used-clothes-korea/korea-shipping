# 管理者メニューの使い方

## 開き方

公開サイトのURL末尾を `admin.html` に変更して開きます。

例：`https://ユーザー名.github.io/リポジトリ名/admin.html`

初期管理者パスワード：`korea_0601`

## 更新手順

1. 管理者メニューで設定を編集します。
2. 「サイトをプレビュー」で表示を確認します。
3. 「保存・出力」から「更新ファイルをダウンロード」を押します。
4. ダウンロードされた `config.js` を、GitHub Desktopで管理しているサイトフォルダーの `assets/js/config.js` へ上書きします。
5. GitHub Desktopで変更をCommitし、Push originを押します。
6. GitHub Pagesへ反映されるまで数分待ちます。

## 下書きとバックアップ

- 「この端末に下書き保存」は、現在使用しているブラウザだけに保存されます。
- 「バックアップをダウンロード」で設定全体をJSONとして保管できます。
- 別の端末では「バックアップを選択」から復元できます。

## 注意

GitHub Pagesは静的サイトのため、管理画面の「保存」だけでは公開サイトは更新されません。必ず `config.js` をGitHub Desktopで上書きし、CommitとPushを行ってください。

管理者ログインは簡易的な閲覧制限です。パスワードや個人情報など、機密情報の保管用途には使用しないでください。
